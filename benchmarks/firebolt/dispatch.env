# Direct-engine suite trigger: any pushed change to this file starts
# .github/workflows/benchmark-firebolt-direct.yml with these parameters.
# Bump RUN to redispatch with unchanged parameters.
TIER=ccx33
LOCATION=nbg1
SMOKE=0
RUN=5
