[package]
name = "drain"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
flate2 = "1"
serde_json = "1"

[profile.release]
lto = true
