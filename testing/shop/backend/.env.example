# Backend OpenTelemetry export target (OTLP/HTTP). Secret token — keep real values in backend/.env (gitignored).
TRACEWAY_URL=https://cloud.tracewayapp.com
TRACEWAY_BACKEND_TOKEN=<project-token>
TRACEWAY_SERVICE_NAME=shop-backend
